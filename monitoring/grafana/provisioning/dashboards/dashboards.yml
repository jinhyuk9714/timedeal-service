# Grafana 대시보드 자동 로드
apiVersion: 1
providers:
  - name: "Timedeal"
    orgId: 1
    folder: "Timedeal"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
